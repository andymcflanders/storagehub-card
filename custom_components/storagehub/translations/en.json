{
  "config": {
    "step": {
      "user": {
        "data": {
          "host": "Host URL",
          "api_key": "API key",
          "verify_ssl": "Verify SSL certificate"
        },
        "data_description": {
          "host": "Base URL of your StorageHub instance, e.g. https://storagehub.example.com",
          "api_key": "API key with read and search scopes (starts with shub_)",
          "verify_ssl": "Turn off if StorageHub uses a self-signed certificate."
        }
      },
      "reauth_confirm": {
        "description": "The StorageHub API key was rejected. Paste a new one to reconnect.",
        "data": {
          "api_key": "API key"
        }
      },
      "reconfigure": {
        "data": {
          "host": "Host URL",
          "api_key": "API key",
          "verify_ssl": "Verify SSL certificate"
        },
        "data_description": {
          "verify_ssl": "Turn off if StorageHub uses a self-signed certificate."
        }
      }
    },
    "error": {
      "cannot_connect": "Failed to connect",
      "invalid_auth": "Invalid authentication",
      "unknown": "Unexpected error"
    },
    "abort": {
      "already_configured": "StorageHub is already configured",
      "instance_mismatch": "The host you supplied points at a different StorageHub instance",
      "reauth_successful": "Re-authentication completed",
      "reconfigure_successful": "Configuration updated"
    }
  },
  "entity": {
    "sensor": {
      "total_items": {
        "name": "Total items"
      },
      "index_etag": {
        "name": "Index ETag"
      }
    },
    "binary_sensor": {
      "connected": {
        "name": "Connected"
      }
    }
  },
  "services": {
    "search": {
      "name": "Search inventory",
      "description": "Search StorageHub for items by name, owner, container, or location.",
      "fields": {
        "query": {
          "name": "Query",
          "description": "Free-text search. Owner names AND-filter the results, so 'Sverre Bukser' returns only Sverre's pants."
        },
        "limit": {
          "name": "Limit",
          "description": "Maximum number of items to return (1–50)."
        }
      }
    },
    "search_lite": {
      "name": "Get cached item index",
      "description": "Returns the cached lightweight item index used by the StorageHub Lovelace card for client-side filtering."
    },
    "refresh_index": {
      "name": "Refresh item index",
      "description": "Forces an immediate refresh of the cached lightweight item index. Useful for debugging."
    }
  },
  "exceptions": {
    "not_configured": {
      "message": "StorageHub is not configured. Add the integration before calling its services."
    },
    "auth_failed": {
      "message": "StorageHub rejected the API key. Reconfigure the integration."
    },
    "cannot_connect": {
      "message": "Could not reach StorageHub."
    },
    "api_error": {
      "message": "StorageHub returned an error: {detail}"
    }
  }
}
