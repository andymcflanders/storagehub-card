pytest-homeassistant-custom-component

# HA's `conversation` component (used by phase 2 voice tests) pulls in
# the assist_pipeline → tts/stt chain. PHCC ships only the bare HA
# core; these are the transitive runtime deps the chain needs.
hassil==2.1.0
home-assistant-intents
mutagen
ha-ffmpeg
pymicro-vad
pyspeex-noise
